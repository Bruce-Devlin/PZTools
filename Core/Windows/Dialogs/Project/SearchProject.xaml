<Window x:Class="PZTools.Core.Windows.Dialogs.Project.SearchProject"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Find in project" Height="620" Width="880" MinHeight="440" MinWidth="660"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource Brush.BgApp}"
        Foreground="{DynamicResource Brush.TextPrimary}" Icon="/Core/Assets/Icons/PZToolsIcon.png">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Margin="0,0,0,18">
            <TextBlock Text="Find in project" Style="{DynamicResource Text.Title}"/>
            <TextBlock x:Name="ProjectLabel" Style="{DynamicResource Text.Muted}" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"/>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBox x:Name="QueryBox" Style="{DynamicResource ModernTextBox}" TextChanged="SearchChanged"
                     ToolTip="Find literal text or a file path" AutomationProperties.Name="Search project"/>
            <Button Grid.Column="1" Content="Search" IsDefault="True" Click="Search_Click" Style="{DynamicResource PrimaryButton}" Margin="10,0,0,0"/>
        </Grid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,14">
            <CheckBox x:Name="MatchCaseBox" Content="Match case" Checked="SearchChanged" Unchecked="SearchChanged"/>
            <CheckBox x:Name="FileNamesBox" Content="File names only" Margin="22,0,0,0" Checked="SearchChanged" Unchecked="SearchChanged"/>
        </StackPanel>
        <Grid Grid.Row="3">
            <DataGrid x:Name="ResultsGrid" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                      CanUserAddRows="False" CanUserDeleteRows="False" HeadersVisibility="Column"
                      EnableRowVirtualization="True" MouseDoubleClick="Results_DoubleClick" PreviewKeyDown="Results_KeyDown">
                <DataGrid.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="Margin" Value="8,0"/>
                        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                        <Setter Property="ToolTip" Value="{Binding Text, RelativeSource={RelativeSource Self}}"/>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="File" Binding="{Binding RelativePath}" Width="2*"/>
                    <DataGridTextColumn Header="Line" Binding="{Binding Line}" Width="55"/>
                    <DataGridTextColumn Header="Match" Binding="{Binding Preview}" Width="3*"/>
                </DataGrid.Columns>
            </DataGrid>
            <TextBlock x:Name="EmptyText" Text="Search Lua, definitions, translations, and other project text."
                       IsHitTestVisible="False" Style="{DynamicResource Text.Muted}" Margin="30"
                       HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"/>
        </Grid>
        <Grid Grid.Row="4" Margin="0,14,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock x:Name="SummaryText" Text="Enter a search term. Results open at the matching line."
                       Style="{DynamicResource Text.Muted}" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Open result" Click="Open_Click" Style="{DynamicResource PrimaryButton}"/>
                <Button Content="Close" IsCancel="True" Style="{DynamicResource ModernButton}" Margin="8,0,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
