<Window x:Class="PZTools.Core.Windows.Dialogs.Project.ProjectSelector"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PZ Tools - Select Project"
        Height="420"
        Width="580"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource Brush.BgApp}"
        Foreground="{DynamicResource Brush.TextPrimary}"
        Icon="/Core/Assets/Icons/PZToolsIcon.png">

    <Grid Margin="18">
        <Border Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" Margin="0,0,0,12">
                    <TextBlock Text="Select a project" Style="{StaticResource Text.Title}"/>
                    <TextBlock Text="Choose a project and target." Style="{StaticResource Text.Muted}" Margin="0,0,0,10"/>
                    <DockPanel LastChildFill="True">
                        <Button x:Name="OpenProjectFolderBtn"
                                DockPanel.Dock="Right"
                                Content="Open folder"
                                Style="{StaticResource CompactButton}"
                                Margin="10,0,0,0"
                                Click="OpenProjectFolderBtn_Click"/>
                        <StackPanel>
                            <TextBlock Text="PROJECTS FOLDER" FontSize="10" Foreground="{DynamicResource Brush.TextMuted}"/>
                            <TextBlock x:Name="ProjectFolderTxt"
                                   Text="Projects folder"
                                   Foreground="{StaticResource Brush.TextInfo}"
                                   TextTrimming="CharacterEllipsis"
                                   ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                                   VerticalAlignment="Center"/>
                        </StackPanel>
                    </DockPanel>

                </StackPanel>

                <!-- TreeView -->
                <Border Grid.Row="1"
                        Background="{StaticResource Brush.BgApp}"
                        BorderBrush="{StaticResource Brush.Border}"
                        BorderThickness="1"
                        CornerRadius="10"
                        Padding="8">

                    <TreeView x:Name="ProjectsTreeView"
                              Style="{StaticResource ModernTreeView}">
                        <TreeView.Resources>
                            <Style TargetType="TreeViewItem" BasedOn="{StaticResource ModernTreeViewItem}"/>
                        </TreeView.Resources>

                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding VersionWrappers}">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>

                                <HierarchicalDataTemplate.ItemTemplate>
                                    <HierarchicalDataTemplate ItemsSource="{Binding Builds}">
                                        <TextBlock Text="{Binding Label}"
                                                   Foreground="{StaticResource Brush.TextInfo}"
                                                   Margin="10,2,0,2"/>

                                        <HierarchicalDataTemplate.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Build, StringFormat='b{0}'}"
                                                           Margin="20,2,0,2"
                                                           Foreground="{StaticResource Brush.TextWarn}"/>
                                            </DataTemplate>
                                        </HierarchicalDataTemplate.ItemTemplate>
                                    </HierarchicalDataTemplate>
                                </HierarchicalDataTemplate.ItemTemplate>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </Border>

                <Border Grid.Row="2"
                        Background="{StaticResource Brush.Border}"
                        Height="1"
                        VerticalAlignment="Top" Margin="0,8,0,0"/>

                <!-- Buttons -->
                <StackPanel Grid.Row="3"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,12,0,0">
                    <Button Style="{StaticResource ModernButton}"
                            Content="New project"
                            Width="110"
                            Margin="0,0,8,0"
                            Click="NewProjectButton_Click"/>

                    <Button Style="{StaticResource ModernButton}"
                            Content="Import mod"
                            Width="110"
                            Margin="0,0,8,0"
                            Click="ImportModButton_Click"/>

                    <Button Style="{StaticResource PrimaryButton}"
                            Content="Open project"
                            Width="115"
                            Margin="0,0,8,0"
                            Click="OkButton_Click"/>

                    <Button Style="{StaticResource ModernButton}"
                            Content="Exit PZ Tools"
                            Width="105"
                            Click="CancelButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
