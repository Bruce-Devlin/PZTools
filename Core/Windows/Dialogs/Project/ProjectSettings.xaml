<Window x:Class="PZTools.Core.Windows.Dialogs.Project.ProjectSettings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PZTools.Core.Windows.Dialogs.Project"
        mc:Ignorable="d"
        Title="Project Settings" Height="565" Width="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E1E">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Project Settings" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Mod Info -->
            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <TextBlock Text="Name:" Foreground="White" Margin="0,0,0,2"/>
                <TextBox x:Name="txtModName" Background="#2D2D30" Foreground="White" BorderBrush="#3E3E42" Margin="0,0,0,10"/>

                <TextBlock Text="ID:" Foreground="White" Margin="0,0,0,2"/>
                <TextBox x:Name="txtModID" Background="#2D2D30" Foreground="White" BorderBrush="#3E3E42" Margin="0,0,0,10"/>

                <TextBlock Text="Description:" Foreground="White" Margin="0,0,0,2"/>
                <TextBox x:Name="txtDescription" Background="#2D2D30" Foreground="White" BorderBrush="#3E3E42"
                         AcceptsReturn="True" TextWrapping="Wrap" Height="150" Margin="0,0,0,10"/>

                <TextBlock Text="Lua Watermark:" Foreground="White" Margin="0,0,0,2"/>
                <TextBox x:Name="txtWatermark" Background="#2D2D30" Foreground="White" BorderBrush="#3E3E42"
                    AcceptsReturn="True" TextWrapping="Wrap" Height="150" Margin="0,0,0,10"/>
                <TextBlock Text="(this can be applied to files via Tools > Lua Watermark)" Foreground="#FF8C8C8C" Margin="0,0,0,2"/>
            </StackPanel>

            <!-- Right Panel: Poster Preview -->
            <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                <TextBlock Text="Poster:" Foreground="White" Margin="0,0,0,5"/>
                <Border BorderBrush="#3E3E42" BorderThickness="1" Width="200" Height="300" Background="#2D2D30">
                    <Image x:Name="imgPoster" Stretch="Uniform"/>
                </Border>
                <Button Content="Change Poster..." Width="150" Margin="0,10,0,0" Click="ChangePoster_Click"
                        Background="#3E3E42" Foreground="White"/>
            </StackPanel>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Cancel" Width="75" Margin="0,0,10,0" Click="CancelButton_Click"
                    Background="#3E3E42" Foreground="White"/>
            <Button Content="Save" Width="75" Click="SaveButton_Click"
                    Background="#007ACC" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>
